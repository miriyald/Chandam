# Baseline images – desktop viewport
#
# On first run use: npm run snapshots
# to generate baseline PNG images for visual regression.
#
# Images are committed to source control so CI comparisons work.
